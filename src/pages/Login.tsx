
import React from 'react';
import { useNavigate } from 'react-router-dom';
import Index from './Index'; // Utiliser la page Index existante comme page de login

const Login = () => {
  return <Index />;
};

export default Login;

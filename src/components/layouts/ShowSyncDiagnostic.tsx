
import React from 'react';
import SyncDiagnosticDialog from '../common/SyncDiagnosticDialog';

const ShowSyncDiagnostic = () => {
  // Hidden as requested, return null so nothing is rendered
  return null;
};

export default ShowSyncDiagnostic;

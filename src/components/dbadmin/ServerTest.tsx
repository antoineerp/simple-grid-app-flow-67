
import React from 'react';
import ServerTest from '../ServerTest'; // Reuse the existing ServerTest component

const ServerTestComponent = () => {
  return <ServerTest />;
};

export default ServerTestComponent;
